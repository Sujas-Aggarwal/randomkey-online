import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function RecoveryKeyPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="recovery-key">
      <PlaceholderTool toolId="recovery-key" />
    </ToolLayout>
  );
}
