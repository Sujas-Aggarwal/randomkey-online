import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function VapidKeyPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="vapid-key">
      <PlaceholderTool toolId="vapid-key" />
    </ToolLayout>
  );
}
