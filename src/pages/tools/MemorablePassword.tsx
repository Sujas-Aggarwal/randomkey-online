import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function MemorablePasswordPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="memorable-password">
      <PlaceholderTool toolId="memorable-password" />
    </ToolLayout>
  );
}
