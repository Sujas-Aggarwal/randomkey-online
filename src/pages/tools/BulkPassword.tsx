import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function BulkPasswordPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="bulk-password">
      <PlaceholderTool toolId="bulk-password" />
    </ToolLayout>
  );
}
