import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function BackupCodesPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="backup-codes">
      <PlaceholderTool toolId="backup-codes" />
    </ToolLayout>
  );
}
