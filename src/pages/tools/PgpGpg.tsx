import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function PgpGpgPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="pgp-gpg">
      <PlaceholderTool toolId="pgp-gpg" />
    </ToolLayout>
  );
}
