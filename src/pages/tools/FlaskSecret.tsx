import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function FlaskSecretPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="flask-secret">
      <PlaceholderTool toolId="flask-secret" />
    </ToolLayout>
  );
}
