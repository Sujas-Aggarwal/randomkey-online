import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function PasswordPresetsPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="password-presets">
      <PlaceholderTool toolId="password-presets" />
    </ToolLayout>
  );
}
