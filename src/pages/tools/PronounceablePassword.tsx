import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function PronounceablePasswordPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="pronounceable-password">
      <PlaceholderTool toolId="pronounceable-password" />
    </ToolLayout>
  );
}
