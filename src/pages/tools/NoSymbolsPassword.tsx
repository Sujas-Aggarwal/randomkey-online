import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function NoSymbolsPasswordPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="no-symbols-password">
      <PlaceholderTool toolId="no-symbols-password" />
    </ToolLayout>
  );
}
