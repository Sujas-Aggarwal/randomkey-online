import { ToolLayout } from "@/layouts/ToolLayout";
import { PlaceholderTool } from "@/pages/tools/PlaceholderTool";

export default function LaravelAppKeyPage(): React.JSX.Element {
  return (
    <ToolLayout toolId="laravel-app-key">
      <PlaceholderTool toolId="laravel-app-key" />
    </ToolLayout>
  );
}
